fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
paho-mqtt>=1.6.1
# BLE stack: bleak (repo root / toolkit requirements)
# Fall detection: install sibling package separately
#   pip install -e ../fall_detection_pi
