# Python bytecode / venv (regenerate via setup_linux.sh)
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
.venv/
venv/
env/

# IDE / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Logs only (keep CSV/db/pair configs — this is a dev share repo)
*.log

# Do not commit machine SSH public key helper
GITHUB_SSH_KEY.md

# Optional huge OS packages if dropped in tree (rare)
*.jmod
