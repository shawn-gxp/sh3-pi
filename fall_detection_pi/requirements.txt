# Standalone fall-detection package (MediaPipe Tasks API)
# mediapipe pulls opencv-contrib-python — do not also pin opencv-python-headless
mediapipe>=0.10.30
numpy>=1.26.0
requests>=2.31.0

# Independent HTTP server (python -m fall_detection_pi.web_server)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
# optional SSL self-signed cert generation
cryptography>=41.0.0

# Dev / unit tests
pytest>=7.0
