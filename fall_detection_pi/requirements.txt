# Standalone fall-detection package (MediaPipe Tasks API)
# mediapipe pulls opencv-contrib-python — do not also pin opencv-python-headless
mediapipe>=0.10.30
numpy>=1.26.0
requests>=2.31.0

# Dev / unit tests
pytest>=7.0
