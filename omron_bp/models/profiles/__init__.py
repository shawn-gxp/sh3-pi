"""
Device profiles live in catalog.py (merged from hass-omron + omblepy).

Per-model override modules can still be added later; register them from
models.registry if needed. Prefer extending catalog.py for bulk coverage.
"""
