bleak>=0.22.0
paho-mqtt>=1.6.1

# Fall detection (MediaPipe Tasks PoseLandmarker + OpenCV)
# mediapipe pulls opencv-contrib-python — do not also pin opencv-python-headless
# (conflicting OpenCV packages break imports).
mediapipe>=0.10.30
numpy>=1.26.0
requests>=2.31.0
